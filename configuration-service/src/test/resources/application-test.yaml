# src/test/resources/application-test.yml
spring:
  cloud:
    consul:
      config:
        enabled: false
  data:
    redis:
      port: 12345

logging:
  level:
    org.springframework.data.redis.cache: DEBUG
